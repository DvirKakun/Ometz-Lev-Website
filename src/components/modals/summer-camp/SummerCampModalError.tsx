import { ModalError } from "../shared";

const SummerCampModalError = () => {
  return (
    <ModalError message="אירעה שגיאה בשליחת ההרשמה. אנא נסו שוב מאוחר יותר." />
  );
};

export default SummerCampModalError;