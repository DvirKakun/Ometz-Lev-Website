import { ModalError } from "../shared";

const ContactModalError = () => {
  return (
    <ModalError message="אירעה שגיאה בשליחת ההודעה. אנא נסו שוב מאוחר יותר." />
  );
};

export default ContactModalError;